//! Binary entry point for `build-release` — not yet ported to Rust.
//!
//! Usage:
//!   cargo run --bin build-release [-- --no-build]
//!
//! This is a stub that prints a message and exits successfully.

fn main() {
    eprintln!("build-release: not yet ported to Rust");
    std::process::exit(0);
}
